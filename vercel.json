{
    "builds": [{
        "src": "core/wsgi.py",
        "use": "@vercel/python3.8.3",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "core/wsgi.py"
        }
    ]
}